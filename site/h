# P8 数传电台

## 产品简介

P8 数传电台是一款专为无人机远程通信设计的无线数据传输模块，负责在飞控与地面站之间建立远距离、低延迟的双向数据链路，实现实时遥测数据下传和地面指令上发。

本项目中 P8 数传电台配合 CUAV X25 EVO 飞控和 QGC 地面站使用，是实现超视距（BVLOS）飞行控制的关键通信链路。

---

## 硬件规格参数

| 项目 | 参数 |
|------|------|
| **产品名称** | P8 数传电台 |
| **工作频段** | 433MHz / 915MHz（区域可选） |
| **发射功率** | 100mW / 500mW / 1W 三档可调 |
| **最大通信距离** | ≤ 15km（视距，1W 满功率） |
| **通信协议** | MAVLink 1 / MAVLink 2 |
| **调制方式** | GFSK / LoRa（扩频可选） |
| **空中速率** | 250 kbps（LoRa 模式） |
| **空中加密** | AES-128 硬件加密 |
| **串口接口** | UART（3.3V TTL）× 1 |
| **串口波特率** | 57600 bps（默认，可调） |
| **USB 接口** | Type-C × 1（地面端连接电脑） |
| **供电电压** | 5V DC（空中端由飞控供电；地面端 USB 供电） |
| **功耗** | ≤ 1.5W（发射态） |
| **工作温度** | -20°C ～ +70°C |
| **尺寸** | 约 45mm × 25mm × 10mm |
| **重量** | 约 15g（含天线接口） |
| **天线接口** | SMA 母座 |
| **天线类型** | 棒状偶极天线（标配），可选玻璃钢高增益天线 |
| **LED 指示** | 红（电源）/ 绿（链路连接）/ 蓝（数据收发） |

---

## 产品特性

| 特性 | 说明 |
|------|------|
| **超远距离** | 1W 满功率视距通信 ≤ 15km，满足 BVLOS 任务需求 |
| **LoRa 扩频** | 可选 LoRa 调制，提升抗干扰能力与通信距离（速率略低） |
| **AES 加密** | 硬件 AES-128 加密，防止通信劫持与数据泄露 |
| **一对多通信** | 支持一个地面端同时监控多台无人机 |
| **MAVLink 透传** | MAVLink v1/v2 原生透传，无需协议转换 |
| **频率自适应** | 检测到干扰时自动跳频，保证链路稳定性 |
| **即插即用** | 地面端 USB 直连 PC，空中端连接飞控 TELEM 口 |
| **OTA 固件升级** | 支持无线固件升级，无需拆卸设备 |

---

## 安装与连接

### 空中端（连接飞控）

| 数传线序 | 功能 | 飞控端连接 | 线色参考 |
|---------|------|-----------|---------|
| VCC (5V) | 供电正极 | TELEM 口 5V 输出 | 红色 |
| GND | 电源地 | TELEM 口 GND | 黑色 |
| TX | 数据发送 | TELEM 口 RX | 白色 / 黄色 |
| RX | 数据接收 | TELEM 口 TX | 绿色 / 蓝色 |

> 空中端由飞控 TELEM 口直接供电（5V），无需独立电源。

### 地面端（连接电脑）

将 P8 地面端通过 Type-C USB 线连接至笔记本电脑，系统自动识别为串口设备（COM 口或 /dev/ttyUSB* 设备）。打开 QGC 地面站，在「通信连接」中选择对应串口，设置波特率为 57600。

---

## 配置步骤

### 频率与功率设置

通过专用配置工具（P8 Config Tool）或 AT 指令进行参数配置：

| 参数 | 可选值 | 推荐值 | 说明 |
|------|--------|--------|------|
| **频率** | 410～490MHz / 902～928MHz | 433MHz / 915MHz | 根据当地法规选择 |
| **发射功率** | 0 / 1 / 2（对应 100mW / 500mW / 1W） | 2（1W） | 远距离任务选最大 |
| **调制方式** | GFSK / LoRa | LoRa | 推荐抗干扰更强的 LoRa |
| **空中速率** | 125k / 250k / 500k bps | 250k | 平衡速率与距离 |
| **串口波特率** | 57600 / 115200 bps | 57600 | 与飞控 TELEM 口保持一致 |

### AT 指令配置示例

```bash
# 查询当前参数
AT+PARAM?

# 设置频率为 915MHz
AT+FREQ=915

# 设置发射功率为 1W（等级 2）
AT+POWER=2

# 设置 LoRa 模式
AT+MOD=1

# 重启生效
AT+REBOOT
```

### QGC 连接配置

1. 打开 QGC → 应用程序设置 → 通信连接
2. 点击「添加」→ 选择「串口」
3. 选择 P8 地面端对应的串口号
4. 波特率设为 `57600`
5. 点击「连接」，等待 MAVLink 心跳检测成功

---

## 指示灯说明

| 指示灯 | 状态 | 含义 |
|--------|------|------|
| 红灯（PWR） | 常亮 | 供电正常 |
| 红灯（PWR） | 熄灭 | 未上电或供电异常 |
| 绿灯（LINK） | 常亮 | 空中端与地面端已配对并建立链路 |
| 绿灯（LINK） | 慢闪 | 正在搜索配对 |
| 绿灯（LINK） | 熄灭 | 未配对或链路断开 |
| 蓝灯（DATA） | 快闪 | MAVLink 数据正在传输 |
| 蓝灯（DATA） | 慢闪或熄灭 | 无数据传输 |

---

## 常见故障排查

| 故障现象 | 可能原因 | 排查方法 |
|---------|---------|---------|
| 地面站无法连接飞控 | 数传未配对、波特率不一致 | 检查两端指示灯是否常绿；确认串口波特率匹配 |
| 通信距离过短 | 功率未设为最大、天线损坏 | 检查功率参数 `AT+POWER` 是否为 2；更换天线测试 |
| 数据传输断续 | 干扰严重、天线受遮挡 | 切换频段或启用 LoRa 模式；调整天线安装位置 |
| QGC 无法识别 COM 口 | 驱动未安装 | 安装 P8 串口驱动（FTDI / CH340，视版本而定） |
| LED 全不亮 | 供电异常 | 检查空中端 TELEM 口 5V 输出；地面端更换 USB 线 |

---

## 相关文档

| 文档 | 说明 |
|------|------|
| [P9 数传电台](telemetry-p9.md) | 升级型号，更远距离 |
| [CUAV X25 EVO 飞控](cuav-x25-evo.md) | 飞控端 TELEM 口配置 |
| [CUAV 2HP RTK](2hp-rtk.md) | RTK 差分数据依赖数传链路 |
| [线路连接图](../wiring/wiring-diagram.md) | 连接示意 |